# Render Blueprint - auto-deploy config
services:
  - type: web
    name: wb-diagram-board-collab-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
